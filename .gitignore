# This repository tracks the firmware source plus the curated hardware
# documentation for THIS fork: my modified CAD, the before/after comparison
# figures, the corrected wiring drawing and the build photography.
#
# Upstream's bulk binary assets (the full printable STL set, the original
# wiring diagrams, the MP3s and the parts list) are large and unchanged from
# upstream, so they are deliberately not tracked. Get them from:
#
#   https://github.com/cguweb-com/Arduino-Projects/tree/main/Nova-SM3
#
Nova-SM3/

# Arduino build output
build/
*.hex
*.elf

# scratch / working files
*.tmp
